1. Build a container for PyTorch with and without GPU support (with CUDA 11.3) using sandbox.
2. Create a definition file and build a container for PyTorch with CUDA 11.3.
3. Convert the Dockerfile (6_docker.txt) to Apptainer and build the container. You will need to fix the definition file.